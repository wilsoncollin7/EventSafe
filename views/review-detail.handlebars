{{> nav/nav-block}}

<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Review</h5>
          <p>
            <strong>ID</strong>: {{review.id}}</p>
          <p>
            <strong>Title</strong>: {{review.title}}</p>
          <p>
            <strong>Location</strong>: <a id="location">{{review.location}}</a></p> 
          <p>
            <strong>Type</strong>: {{review.type}}</p> 
          <p>
            <strong>Description:</strong> {{review.description}}</p>
          <div id="map"></div>
            <a href="/dashboard">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>

{{> modal/login-block}}
